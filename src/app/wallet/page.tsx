"use client";
import Link from "next/link";
import Header from "@/components/layout/Header";
import Container from "@/components/ui/Container";
import { useWallet } from "@/hooks/useWallet";
export default function WalletPage() { const { balance, rewardPoints, transactions } = useWallet(); return <div className="min-h-screen bg-[var(--background)]"><Header /><Container className="py-6"><Link href="/account" className="text-xs font-black text-[var(--primary)]">← Account</Link><h1 className="mt-3 text-3xl font-black">Wallet & rewards</h1><section className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-[var(--primary)] p-5 text-white"><p className="text-xs font-bold">Wallet balance</p><p className="mt-2 text-2xl font-black">₹{balance}</p></div><div className="rounded-2xl bg-white p-5"><p className="text-xs font-bold text-[var(--text-muted)]">Reward points</p><p className="mt-2 text-2xl font-black">{rewardPoints}</p></div></section><section className="mt-6 rounded-2xl bg-white p-5"><h2 className="font-black">Reward history</h2>{transactions.length ? <div className="mt-3 space-y-3">{transactions.map((item) => <div key={item.id} className="flex justify-between text-sm"><span>{item.note}</span><b className="text-[var(--success)]">+{item.points} points</b></div>)}</div> : <p className="mt-3 text-sm text-[var(--text-muted)]">Completed orders पर points यहाँ दिखेंगे।</p>}</section></Container></div>; }
